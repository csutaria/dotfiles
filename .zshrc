# Source common settings
if [[ -f ~/.zshrc_common ]]; then
  source ~/.zshrc_common
fi

# Source local overrides
if [[ -f ~/.zshrc_local ]]; then
  source ~/.zshrc_local
fi

# ▼ Junk automatically added by installers below ▼

